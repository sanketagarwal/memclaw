# dependencies
node_modules/

# env / secrets
.env
.env.local
*.local
# MCP config can contain tokens — keep the .example, ignore the real one
memclaw.mcp.json

# mastra build + local stores
.mastra/
dist/
*.db
*.db-journal
*.db-wal
*.db-shm
mastra.db*
*.duckdb
*.duckdb.wal
*.wal

# pubsub unix socket
/tmp/mastra/

# agent workspace (local files the agent creates at runtime)
/workspace/
/scratch-workspace/

# logs / os
*.log
.DS_Store

# local scratch files
scratch-*
